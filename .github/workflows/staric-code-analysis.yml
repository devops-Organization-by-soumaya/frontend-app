# ✅ Name of the workflow
name: Pipeline CI/CD sonarqube-code-quality-scan-job

# ✅ Trigger this workflow on pull requests to the main branch
on:
  pull_request:
    branches:
      - main

jobs:
  # 🔍 Job responsible for running the SonarQube code quality analysis
  sonarqube-code-quality-scan-job:

    # 🖥️ Run the job on the latest Ubuntu runner provided by GitHub
    runs-on: ubuntu-latest

    steps:

      # 📥 Step 1: Clone the source code from the repository
      - name: Clone Source Code
        uses: actions/checkout@v4
        with:
          # Needed so SonarQube can do accurate blame/history analysis
          fetch-depth: 0

      # 🟢 Step 2: Set up the Node.js environment
      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      # 📦 Step 3: Install all project dependencies
      - name: Install dependencies
        run: npm install

      # 🔎 Step 4: Run SonarQube code quality and security analysis
      - name: SonarQube Scan
        uses: sonarsource/sonarqube-scan-action@master
        env:
          # 🔐 Authentication token used to connect to SonarCloud
          # SONAR_HOST_URL removed — it was overriding sonar.host.url
          # in sonar-project.properties with an empty value, causing
          # "URI with undefined scheme"
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}